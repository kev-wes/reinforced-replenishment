# Here you can define credentials for different data sets and environment.
#
#
# Example:
#
# dev_s3:
#     aws_access_key_id: token
#     aws_secret_access_key: key
#
# prod_s3:
#     aws_access_key_id: token
#     aws_secret_access_key: key
#
# dev_sql:
#     username: admin
#     password: admin

db_credentials:
  con: ${oc.env:POSTGRES_CONNECTION_STRING}

data_platform_postgres_connection_string: 
  con: ${oc.env:DATA_PLATFORM_POSTGRES_CONNECTION_STRING}

az_storage_credentials: 
  connection_string: ${oc.env:BLOB_STORAGE_CONNECTION_STRING}